create or replace TABLE DATAVAULT_SHARED.ECORE_SOURCE_DATA.TRIAL_BALANCE (
	GLACCTDISP1 VARCHAR(16777216),
	GLACCTDESC1 VARCHAR(16777216),
	FISCALYEAROB NUMBER(38,2),
	FISCALPEROB NUMBER(38,2),
	DEBIT1 VARCHAR(16777216),
	CREDIT1 VARCHAR(16777216),
	TFISCALPERCB NUMBER(38,2),
	PERSTARTDATE DATE,
	PERENDDATE DATE
);

ALTER TABLE DATAVAULT_SHARED.ECORE_SOURCE_DATA.TRIAL_BALANCE
ALTER GLACCTDISP1 SET DATA TYPE VARCHAR(20)
;

ALTER TABLE DATAVAULT_SHARED.ECORE_SOURCE_DATA.TRIAL_BALANCE
ALTER GLACCTDESC1 SET DATA TYPE VARCHAR(100)
;

ALTER TABLE DATAVAULT_SHARED.ECORE_SOURCE_DATA.TRIAL_BALANCE
ALTER DEBIT1 SET DATA TYPE VARCHAR(50)
;

ALTER TABLE DATAVAULT_SHARED.ECORE_SOURCE_DATA.TRIAL_BALANCE
ALTER CREDIT1 SET DATA TYPE VARCHAR(50)
;

-- 14
SELECT MAX(LEN(GLACCTDISP1))
FROM DATAVAULT_SHARED.ECORE_SOURCE_DATA.TRIAL_BALANCE
;

-- 50
SELECT MAX(LEN(GLACCTDESC1))
FROM DATAVAULT_SHARED.ECORE_SOURCE_DATA.TRIAL_BALANCE
;

-- 11
SELECT MAX(LEN(DEBIT1))
FROM DATAVAULT_SHARED.ECORE_SOURCE_DATA.TRIAL_BALANCE
;

-- 11
SELECT MAX(LEN(CREDIT1))
FROM DATAVAULT_SHARED.ECORE_SOURCE_DATA.TRIAL_BALANCE
;

SELECT DEBIT1, TO_NUMBER(DEBIT1)
FROM DATAVAULT_SHARED.ECORE_SOURCE_DATA.TRIAL_BALANCE
ORDER BY TO_NUMBER(DEBIT1) DESC
;

SELECT CREDIT1, TO_NUMBER(CREDIT1)
FROM DATAVAULT_SHARED.ECORE_SOURCE_DATA.TRIAL_BALANCE
ORDER BY TO_NUMBER(DEBIT1) DESC
;

SELECT '225,936,518,469,197.08', TO_NUMBER('225936518469197.08')
FROM DUAL
;
