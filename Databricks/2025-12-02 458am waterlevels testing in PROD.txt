CREATE TABLE production.meta.waterlevels_20251202
AS SELECT * FROM production.meta.waterlevels
;

SELECT * FROM production.meta.waterlevels
ORDER BY STARTED_AT_TS DESC, TABLE_NAME ASC
;

SELECT *
FROM production.bronze.raw_production_process
WHERE TO_DATE(EXTRACT_DATE) > TO_DATE('2025-11-20')
ORDER BY EXTRACT_DATE ASC
;

SELECT *
FROM production.bronze.stg_production_process
ORDER BY EXTRACT_DATE DESC
;

SELECT
                WATERLEVEL_TS,
                ROW_NUMBER() OVER (PARTITION BY TABLE_NAME ORDER BY WATERLEVEL_TS DESC) AS rn
              FROM
                `production`.`meta`.`waterlevels`
              WHERE
                TABLE_NAME = '`production`.`bronze`.`stg_production_process`'
                AND WATERLEVEL_TS IS NOT NULL
;

SELECT *
FROM production.silver.sat_production_process
ORDER BY EXTRACT_DATE DESC
;

(
  -- Generated by AutomateDV (formerly known as dbtvault)
  -- depends_on: `production`.`meta`.`waterlevels`
  WITH source_data AS (
    SELECT
      PART_NUMBER,
      JOB_NUMBER,
      RESOURCEID,
      RESOURCEGRPID,
      LABORDTL_ASSEMBLYSEQ,
      LABORDTL_OPRSEQ,
      LABORDTL_OPCODE,
      LABORDTL_LABORQTY,
      JOBOPER_IUM,
      JOBOPER_RUNQTY,
      JOBOPER_ESTPRODHOURS,
      LABORDTL_CLOCKINDATE,
      LABORDTL_DSPCLOCKINTIME,
      LABORDTL_DSPCLOCKOUTTIME,
      LABORDTL_EMPLOYEENUM,
      LABORDTL_SHIFT,
      LABORDTL_LABORTYPE,
      LABORDTL_EARNEDHRS,
      LABORDTL_LABORHRS,
      LABORDTL_BURDENHRS,
      LABORDTL_REWORK,
      LABORDTL_REWORKREASONCODE,
      CALCULATED_WEIGHTLBS,
      CALCULATED_WEIGHTLBS2,
      EXTRACT_DATE
    FROM
      `production`.`bronze`.`raw_production_process`
    WHERE
      TO_TIMESTAMP(EXTRACT_DATE) > TO_TIMESTAMP(
        (
          SELECT
            rnselect.WATERLEVEL_TS
          FROM
            (
              SELECT
                WATERLEVEL_TS,
                ROW_NUMBER() OVER (PARTITION BY TABLE_NAME ORDER BY WATERLEVEL_TS DESC) AS rn
              FROM
                `production`.`meta`.`waterlevels`
              WHERE
                TABLE_NAME = '`production`.`bronze`.`stg_production_process`'
                AND WATERLEVEL_TS IS NOT NULL
            ) rnselect
          WHERE
            rnselect.rn = 1
        )
      )
  ),
  derived_columns AS (
    SELECT
      PART_NUMBER,
      JOB_NUMBER,
      RESOURCEID,
      RESOURCEGRPID,
      LABORDTL_ASSEMBLYSEQ,
      LABORDTL_OPRSEQ,
      LABORDTL_OPCODE,
      LABORDTL_LABORQTY,
      JOBOPER_IUM,
      JOBOPER_RUNQTY,
      JOBOPER_ESTPRODHOURS,
      LABORDTL_CLOCKINDATE,
      LABORDTL_DSPCLOCKINTIME,
      LABORDTL_DSPCLOCKOUTTIME,
      LABORDTL_EMPLOYEENUM,
      LABORDTL_SHIFT,
      LABORDTL_LABORTYPE,
      LABORDTL_EARNEDHRS,
      LABORDTL_LABORHRS,
      LABORDTL_BURDENHRS,
      LABORDTL_REWORK,
      LABORDTL_REWORKREASONCODE,
      CALCULATED_WEIGHTLBS,
      CALCULATED_WEIGHTLBS2,
      EXTRACT_DATE,
      TO_TIMESTAMP('2025-12-02 11:34:31.175285') AS LOAD_DATETIME,
      TO_TIMESTAMP('1900-01-01 00:00:00.000000') AS START_DATE,
      TO_TIMESTAMP('9999-12-31 23:59:59.999999') AS END_DATE,
      TO_TIMESTAMP('1900-01-01 00:00:00.000000') AS EFFECTIVE_FROM,
      'MANUFACTURING.RESGRPEFF' AS RECORD_SOURCE,
      'MANUFACTURING.RESGRPEFF' AS COLLISION_KEY,
      CAST(LABORDTL_CLOCKINDATE AS string) AS LABORDTL_CLOCKINDATE_STRING,
      TO_TIMESTAMP(
        CONCAT(LABORDTL_CLOCKINDATE_STRING, ' ', LABORDTL_DSPCLOCKINTIME)
      ) AS CLOCKINDATETIME
    FROM
      source_data
  ),
  hashed_columns AS (
    SELECT
      PART_NUMBER,
      JOB_NUMBER,
      RESOURCEID,
      RESOURCEGRPID,
      LABORDTL_ASSEMBLYSEQ,
      LABORDTL_OPRSEQ,
      LABORDTL_OPCODE,
      LABORDTL_LABORQTY,
      JOBOPER_IUM,
      JOBOPER_RUNQTY,
      JOBOPER_ESTPRODHOURS,
      LABORDTL_CLOCKINDATE,
      LABORDTL_DSPCLOCKINTIME,
      LABORDTL_DSPCLOCKOUTTIME,
      LABORDTL_EMPLOYEENUM,
      LABORDTL_SHIFT,
      LABORDTL_LABORTYPE,
      LABORDTL_EARNEDHRS,
      LABORDTL_LABORHRS,
      LABORDTL_BURDENHRS,
      LABORDTL_REWORK,
      LABORDTL_REWORKREASONCODE,
      CALCULATED_WEIGHTLBS,
      CALCULATED_WEIGHTLBS2,
      EXTRACT_DATE,
      LOAD_DATETIME,
      START_DATE,
      END_DATE,
      EFFECTIVE_FROM,
      RECORD_SOURCE,
      COLLISION_KEY,
      LABORDTL_CLOCKINDATE_STRING,
      CLOCKINDATETIME,
      UNHEX(
        MD5(
          NULLIF(
            CONCAT_WS(
              '||',
              IFNULL(NULLIF(UPPER(TRIM(CAST(COLLISION_KEY AS VARCHAR(16)))), ''), '^^'),
              IFNULL(NULLIF(UPPER(TRIM(CAST(JOB_NUMBER AS VARCHAR(16)))), ''), '^^')
            ),
            '^^||^^'
          )
        )
      ) AS JOB_HK,
      UNHEX(
        MD5(
          NULLIF(
            CONCAT_WS(
              '||',
              IFNULL(NULLIF(UPPER(TRIM(CAST(COLLISION_KEY AS VARCHAR(16)))), ''), '^^'),
              IFNULL(NULLIF(UPPER(TRIM(CAST(RESOURCEID AS VARCHAR(16)))), ''), '^^')
            ),
            '^^||^^'
          )
        )
      ) AS RESOURCE_HK,
      UNHEX(
        MD5(
          NULLIF(
            CONCAT_WS(
              '||',
              IFNULL(NULLIF(UPPER(TRIM(CAST(COLLISION_KEY AS VARCHAR(16)))), ''), '^^'),
              IFNULL(NULLIF(UPPER(TRIM(CAST(RESOURCEGRPID AS VARCHAR(16)))), ''), '^^')
            ),
            '^^||^^'
          )
        )
      ) AS RESOURCE_GROUP_HK,
      UNHEX(
        MD5(
          NULLIF(
            CONCAT_WS(
              '||',
              IFNULL(NULLIF(UPPER(TRIM(CAST(COLLISION_KEY AS VARCHAR(16)))), ''), '^^'),
              IFNULL(NULLIF(UPPER(TRIM(CAST(PART_NUMBER AS VARCHAR(16)))), ''), '^^')
            ),
            '^^||^^'
          )
        )
      ) AS INVENTORY_ITEM_HK,
      UNHEX(
        MD5(
          NULLIF(
            CONCAT_WS(
              '||',
              IFNULL(NULLIF(UPPER(TRIM(CAST(COLLISION_KEY AS VARCHAR(16)))), ''), '^^'),
              IFNULL(NULLIF(UPPER(TRIM(CAST(JOB_NUMBER AS VARCHAR(16)))), ''), '^^'),
              IFNULL(NULLIF(UPPER(TRIM(CAST(COLLISION_KEY AS VARCHAR(16)))), ''), '^^'),
              IFNULL(NULLIF(UPPER(TRIM(CAST(RESOURCEID AS VARCHAR(16)))), ''), '^^'),
              IFNULL(NULLIF(UPPER(TRIM(CAST(COLLISION_KEY AS VARCHAR(16)))), ''), '^^'),
              IFNULL(NULLIF(UPPER(TRIM(CAST(RESOURCEGRPID AS VARCHAR(16)))), ''), '^^'),
              IFNULL(NULLIF(UPPER(TRIM(CAST(COLLISION_KEY AS VARCHAR(16)))), ''), '^^'),
              IFNULL(NULLIF(UPPER(TRIM(CAST(PART_NUMBER AS VARCHAR(16)))), ''), '^^'),
              IFNULL(NULLIF(UPPER(TRIM(CAST(LABORDTL_DSPCLOCKINTIME AS VARCHAR(16)))), ''), '^^'),
              IFNULL(NULLIF(UPPER(TRIM(CAST(LABORDTL_CLOCKINDATE AS VARCHAR(16)))), ''), '^^')
            ),
            '^^||^^||^^||^^||^^||^^||^^||^^||^^||^^'
          )
        )
      ) AS PRODUCTION_PROCESS_LHK,
      UNHEX(
        MD5(
          CONCAT_WS(
            '||',
            IFNULL(NULLIF(UPPER(TRIM(CAST(CALCULATED_WEIGHTLBS AS VARCHAR(16)))), ''), '^^'),
            IFNULL(NULLIF(UPPER(TRIM(CAST(CALCULATED_WEIGHTLBS2 AS VARCHAR(16)))), ''), '^^'),
            IFNULL(NULLIF(UPPER(TRIM(CAST(JOBOPER_ESTPRODHOURS AS VARCHAR(16)))), ''), '^^'),
            IFNULL(NULLIF(UPPER(TRIM(CAST(JOBOPER_IUM AS VARCHAR(16)))), ''), '^^'),
            IFNULL(NULLIF(UPPER(TRIM(CAST(JOBOPER_RUNQTY AS VARCHAR(16)))), ''), '^^'),
            IFNULL(NULLIF(UPPER(TRIM(CAST(LABORDTL_ASSEMBLYSEQ AS VARCHAR(16)))), ''), '^^'),
            IFNULL(NULLIF(UPPER(TRIM(CAST(LABORDTL_BURDENHRS AS VARCHAR(16)))), ''), '^^'),
            IFNULL(NULLIF(UPPER(TRIM(CAST(LABORDTL_CLOCKINDATE AS VARCHAR(16)))), ''), '^^'),
            IFNULL(NULLIF(UPPER(TRIM(CAST(LABORDTL_DSPCLOCKINTIME AS VARCHAR(16)))), ''), '^^'),
            IFNULL(NULLIF(UPPER(TRIM(CAST(LABORDTL_DSPCLOCKOUTTIME AS VARCHAR(16)))), ''), '^^'),
            IFNULL(NULLIF(UPPER(TRIM(CAST(LABORDTL_EARNEDHRS AS VARCHAR(16)))), ''), '^^'),
            IFNULL(NULLIF(UPPER(TRIM(CAST(LABORDTL_EMPLOYEENUM AS VARCHAR(16)))), ''), '^^'),
            IFNULL(NULLIF(UPPER(TRIM(CAST(LABORDTL_LABORHRS AS VARCHAR(16)))), ''), '^^'),
            IFNULL(NULLIF(UPPER(TRIM(CAST(LABORDTL_LABORQTY AS VARCHAR(16)))), ''), '^^'),
            IFNULL(NULLIF(UPPER(TRIM(CAST(LABORDTL_LABORTYPE AS VARCHAR(16)))), ''), '^^'),
            IFNULL(NULLIF(UPPER(TRIM(CAST(LABORDTL_OPCODE AS VARCHAR(16)))), ''), '^^'),
            IFNULL(NULLIF(UPPER(TRIM(CAST(LABORDTL_OPRSEQ AS VARCHAR(16)))), ''), '^^'),
            IFNULL(NULLIF(UPPER(TRIM(CAST(LABORDTL_REWORK AS VARCHAR(16)))), ''), '^^'),
            IFNULL(NULLIF(UPPER(TRIM(CAST(LABORDTL_REWORKREASONCODE AS VARCHAR(16)))), ''), '^^'),
            IFNULL(NULLIF(UPPER(TRIM(CAST(LABORDTL_SHIFT AS VARCHAR(16)))), ''), '^^')
          )
        )
      ) AS PRODUCTION_PROCESS_HASHDIFF
    FROM
      derived_columns
  ),
  columns_to_select AS (
    SELECT
      PART_NUMBER,
      JOB_NUMBER,
      RESOURCEID,
      RESOURCEGRPID,
      LABORDTL_ASSEMBLYSEQ,
      LABORDTL_OPRSEQ,
      LABORDTL_OPCODE,
      LABORDTL_LABORQTY,
      JOBOPER_IUM,
      JOBOPER_RUNQTY,
      JOBOPER_ESTPRODHOURS,
      LABORDTL_CLOCKINDATE,
      LABORDTL_DSPCLOCKINTIME,
      LABORDTL_DSPCLOCKOUTTIME,
      LABORDTL_EMPLOYEENUM,
      LABORDTL_SHIFT,
      LABORDTL_LABORTYPE,
      LABORDTL_EARNEDHRS,
      LABORDTL_LABORHRS,
      LABORDTL_BURDENHRS,
      LABORDTL_REWORK,
      LABORDTL_REWORKREASONCODE,
      CALCULATED_WEIGHTLBS,
      CALCULATED_WEIGHTLBS2,
      EXTRACT_DATE,
      LOAD_DATETIME,
      START_DATE,
      END_DATE,
      EFFECTIVE_FROM,
      RECORD_SOURCE,
      COLLISION_KEY,
      LABORDTL_CLOCKINDATE_STRING,
      CLOCKINDATETIME,
      JOB_HK,
      RESOURCE_HK,
      RESOURCE_GROUP_HK,
      INVENTORY_ITEM_HK,
      PRODUCTION_PROCESS_LHK,
      PRODUCTION_PROCESS_HASHDIFF
    FROM
      hashed_columns
  )
  SELECT
    *
  FROM
    columns_to_select
)
;

SELECT TO_TIMESTAMP('2025-12-02 08:05:02.000000+00:00')
;

