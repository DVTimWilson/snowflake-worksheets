WITH mart_inventory_stock_levels AS (
  SELECT 
        UNIQUE_KEY
      , DATE
      , INVENTORY_ITEM_ID
      , INVENTORY_ITEM_DESCRIPTION
      , CLASS_ID
      , PROD_CODE
      , COREVIEW_PRODUCT_CATEGORY
      , ENTITY
      , BU
      , SITE
      , WAREHOUSE_CODE
      , EXTENDED_COST
      , UNIT_COST
      , UOM
      , QUANTITY
      , UNIT_WEIGHT
      , WEIGHT_LBS
      , IS_ACQUIRED_DATE
      , IS_ACQUIRED_NOW
      , META_LOAD_DATETIME
      , RECORD_SOURCE
--      , LEAD(DATE) OVER (PARTITION BY INVENTORY_ITEM_ID ORDER BY DATE) AS NEXTDATE
  FROM `uat`.`silver`.`bsat_combined_inventory_stock_levels`
  WHERE DATE IS NOT NULL
),

mart_inventory_usage AS (
  SELECT 
      UNIQUE_KEY
    , COMPANY
    , TRANNUM
    , PARTNUM
    , WAREHOUSECODE
    , BINNUM
    , TRANCLASS
    , TRANTYPE
    , INVENTORYTRANS
    , TRANDATE
    , CASE WHEN TRANCLASS = 'I' THEN TRANQTY * -1 ELSE TRANQTY END AS TRANQTY
    , UM
    , JOBNUM
    , PACKLINE
    , PONUM
    , PACKNUM
    , POLINE
    , PORELNUM
    , ORDERNUM
    , ORDERLINE
    , ORDERRELNUM
    , BINNUM2
    , PARTDESCRIPTION
    , REVISIONNUM
    , VENDORNUM
    , INVOICENUM
    , INVOICELINE
    , FISCALYEAR
    , FISCALPERIOD
    , SYSDATE
    , SYSROWID
    , MTLUNITCOST
    , LBRUNITCOST
    , BURUNITCOST
    , SUBUNITCOST
    , MTLBURUNITCOST
    , EXTCOST
    , COSTMETHOD
    , INVADJSRC
    , INVADJREASON
    , PLANT
    , GLTRANS
    , POSTEDTOGL
    , RECORD_SOURCE
    , SITE
    , QUICKBOOKSITEMTYPE
    , QUICKBOOKSNAME
    , QUICKBOOKSMEMO
FROM `uat`.`silver`.`bsat_combined_inventory_transactions`
)

SELECT DATE, ANY_VALUE(INVENTORY_ITEM_ID) AS INVENTORY_ITEM_ID, ANY_VALUE(OpeningStock) AS OpeningStock, ANY_VALUE(ClosingStock) AS ClosingStock
-- , SUM(STOCK_MOVEMENT) AS STOCK_MOVEMENT
, ANY_VALUE(ClosingStock) - ANY_VALUE(OpeningStock) AS STOCK_MOVEMENT
, ANY_VALUE(OpeningStock) + SUM(TRANQTYR) + SUM(TRANQTYI) + SUM(TRANQTYA) - ANY_VALUE(ClosingStock) AS ADJUSTMENTSBALANCING
, SUM(TRANQTYR) AS RECEIPT, SUM(TRANQTYI) AS ISSUE, SUM(TRANQTYA) AS ADJUSTMENTS
FROM (
SELECT date_day
, sO.DATE AS DATE
, sO.INVENTORY_ITEM_ID
-- , s.QUANTITY
, sO.QUANTITY - LAG(sO.QUANTITY) OVER (PARTITION BY sO.INVENTORY_ITEM_ID ORDER BY sO.DATE) AS STOCK_MOVEMENT
, sO.QUANTITY AS OpeningStock
, sC.QUANTITY AS ClosingStock
, COALESCE(tR.TRANQTY, 0) AS TRANQTYR
, COALESCE(tI.TRANQTY, 0) AS TRANQTYI
, COALESCE(tA.TRANQTY, 0) AS TRANQTYA
, LAG(sO.DATE) OVER (PARTITION BY sO.INVENTORY_ITEM_ID ORDER BY sO.DATE) AS PREV_DATE
, SO.SITE
FROM uat.silver.dim_calendar_usa AS c

LEFT OUTER JOIN mart_inventory_stock_levels AS sO
ON date_day - 1 = sO.DATE

LEFT OUTER JOIN mart_inventory_stock_levels AS sC
ON date_day = sC.DATE
AND sO.INVENTORY_ITEM_ID = sC.INVENTORY_ITEM_ID

-- LEFT OUTER JOIN mart_inventory_usage AS tR
LEFT OUTER JOIN (SELECT PARTNUM, TRANDATE, SITE, COMPANY, SUM(TRANQTY) AS TRANQTY FROM mart_inventory_usage WHERE TRANCLASS = 'R' GROUP BY PARTNUM, TRANDATE, SITE, COMPANY) AS tR
ON sO.INVENTORY_ITEM_ID = tR.PARTNUM
-- AND s.DATE = TO_DATE(tR.TRANDATE)
AND date_day - 1 = TO_DATE(tR.TRANDATE)
AND sO.SITE = tR.SITE
AND sO.ENTITY = tR.COMPANY
-- AND tR.TRANCLASS = 'R'

-- LEFT OUTER JOIN mart_inventory_usage AS tI
LEFT OUTER JOIN (SELECT PARTNUM, TRANDATE, SITE, COMPANY, SUM(TRANQTY) AS TRANQTY FROM mart_inventory_usage WHERE TRANCLASS = 'I' GROUP BY PARTNUM, TRANDATE, SITE, COMPANY) AS tI
ON sO.INVENTORY_ITEM_ID = tI.PARTNUM
-- AND s.DATE = TO_DATE(tI.TRANDATE)
AND date_day - 1 = TO_DATE(tI.TRANDATE)
AND sO.SITE = tI.SITE
AND sO.ENTITY = tI.COMPANY
-- AND tI.TRANCLASS = 'I'

-- LEFT OUTER JOIN mart_inventory_usage AS tA
LEFT OUTER JOIN (SELECT PARTNUM, TRANDATE, SITE, COMPANY, SUM(TRANQTY) AS TRANQTY FROM mart_inventory_usage WHERE TRANCLASS = 'A' AND TRANTYPE != 'MFG-VAR' GROUP BY PARTNUM, TRANDATE, SITE, COMPANY) AS tA
ON sO.INVENTORY_ITEM_ID = tA.PARTNUM
-- AND s.DATE = TO_DATE(tA.TRANDATE)
AND date_day - 1 = TO_DATE(tA.TRANDATE)
AND sO.SITE = tA.SITE
AND sO.ENTITY = tA.COMPANY
-- AND tA.TRANCLASS = 'A'
-- AND tA.TRANTYPE != 'MFG-VAR'

WHERE 1 = 1 
AND sO.INVENTORY_ITEM_ID = 'L000036-04-89-1903'
-- AND sC.INVENTORY_ITEM_ID = 'L000036-04-89-1903'
-- WHERE s.INVENTORY_ITEM_ID = '1009015-06-01-0400'
-- WHERE s.INVENTORY_ITEM_ID = '1002529-04-01-1006'
-- WHERE s.INVENTORY_ITEM_ID IN (SELECT DISTINCT PARTNUM FROM mart_inventory_usage)
ORDER BY sO.INVENTORY_ITEM_ID, sO.DATE

) AS x
GROUP BY INVENTORY_ITEM_ID, x.DATE
-- HAVING NOT (STOCK_MOVEMENT = 0 AND TRANSACTION_QUANTITY = 0)
ORDER BY INVENTORY_ITEM_ID, DATE
;

WITH mart_inventory_stock_levels AS (
  SELECT 
        UNIQUE_KEY
      , DATE
      , INVENTORY_ITEM_ID
      , INVENTORY_ITEM_DESCRIPTION
      , CLASS_ID
      , PROD_CODE
      , COREVIEW_PRODUCT_CATEGORY
      , ENTITY
      , BU
      , SITE
      , WAREHOUSE_CODE
      , EXTENDED_COST
      , UNIT_COST
      , UOM
      , QUANTITY
      , UNIT_WEIGHT
      , WEIGHT_LBS
      , IS_ACQUIRED_DATE
      , IS_ACQUIRED_NOW
      , META_LOAD_DATETIME
      , RECORD_SOURCE
--      , LEAD(DATE) OVER (PARTITION BY INVENTORY_ITEM_ID ORDER BY DATE) AS NEXTDATE
  FROM `uat`.`silver`.`bsat_combined_inventory_stock_levels`
  WHERE DATE IS NOT NULL
),

mart_inventory_usage AS (
  SELECT 
      UNIQUE_KEY
    , COMPANY
    , TRANNUM
    , PARTNUM
    , WAREHOUSECODE
    , BINNUM
    , TRANCLASS
    , TRANTYPE
    , INVENTORYTRANS
    , TRANDATE
    , CASE WHEN TRANCLASS = 'I' THEN TRANQTY * -1 ELSE TRANQTY END AS TRANQTY
    , UM
    , JOBNUM
    , PACKLINE
    , PONUM
    , PACKNUM
    , POLINE
    , PORELNUM
    , ORDERNUM
    , ORDERLINE
    , ORDERRELNUM
    , BINNUM2
    , PARTDESCRIPTION
    , REVISIONNUM
    , VENDORNUM
    , INVOICENUM
    , INVOICELINE
    , FISCALYEAR
    , FISCALPERIOD
    , SYSDATE
    , SYSROWID
    , MTLUNITCOST
    , LBRUNITCOST
    , BURUNITCOST
    , SUBUNITCOST
    , MTLBURUNITCOST
    , EXTCOST
    , COSTMETHOD
    , INVADJSRC
    , INVADJREASON
    , PLANT
    , GLTRANS
    , POSTEDTOGL
    , RECORD_SOURCE
    , SITE
    , QUICKBOOKSITEMTYPE
    , QUICKBOOKSNAME
    , QUICKBOOKSMEMO
FROM `uat`.`silver`.`bsat_combined_inventory_transactions`
)

SELECT ENTITY, SITE, INVENTORY_ITEM_ID, SUM(ADJUSTMENTSBALANCING) AS TOTAL_ADJUSTMENTSBALANCING
FROM (
SELECT DATE
, ANY_VALUE(ENTITY) AS ENTITY
, ANY_VALUE(SITE) AS SITE
, ANY_VALUE(INVENTORY_ITEM_ID) AS INVENTORY_ITEM_ID, ANY_VALUE(OpeningStock) AS OpeningStock, ANY_VALUE(ClosingStock) AS ClosingStock
-- , SUM(STOCK_MOVEMENT) AS STOCK_MOVEMENT
, ANY_VALUE(ClosingStock) - ANY_VALUE(OpeningStock) AS STOCK_MOVEMENT
, ANY_VALUE(OpeningStock) + SUM(TRANQTYR) + SUM(TRANQTYI) + SUM(TRANQTYA) - ANY_VALUE(ClosingStock) AS ADJUSTMENTSBALANCING
, SUM(TRANQTYR) AS RECEIPT, SUM(TRANQTYI) AS ISSUE, SUM(TRANQTYA) AS ADJUSTMENTS
FROM (
SELECT date_day
, sO.DATE AS DATE
, sO.INVENTORY_ITEM_ID
-- , s.QUANTITY
, sO.QUANTITY - LAG(sO.QUANTITY) OVER (PARTITION BY sO.INVENTORY_ITEM_ID ORDER BY sO.DATE) AS STOCK_MOVEMENT
, sO.QUANTITY AS OpeningStock
, sC.QUANTITY AS ClosingStock
, COALESCE(tR.TRANQTY, 0) AS TRANQTYR
, COALESCE(tI.TRANQTY, 0) AS TRANQTYI
, COALESCE(tA.TRANQTY, 0) AS TRANQTYA
, LAG(sO.DATE) OVER (PARTITION BY sO.INVENTORY_ITEM_ID ORDER BY sO.DATE) AS PREV_DATE
, sO.ENTITY
, sO.SITE
FROM uat.silver.dim_calendar_usa AS c

LEFT OUTER JOIN mart_inventory_stock_levels AS sO
ON date_day - 1 = sO.DATE

LEFT OUTER JOIN mart_inventory_stock_levels AS sC
ON date_day = sC.DATE
AND sO.INVENTORY_ITEM_ID = sC.INVENTORY_ITEM_ID
AND sO.SITE = sC.SITE
AND sO.ENTITY = sC.ENTITY

-- LEFT OUTER JOIN mart_inventory_usage AS tR
LEFT OUTER JOIN (SELECT PARTNUM, TRANDATE, SITE, COMPANY, SUM(TRANQTY) AS TRANQTY FROM mart_inventory_usage WHERE TRANCLASS = 'R' GROUP BY PARTNUM, TRANDATE, SITE, COMPANY) AS tR
ON sO.INVENTORY_ITEM_ID = tR.PARTNUM
-- AND s.DATE = TO_DATE(tR.TRANDATE)
AND date_day - 1 = TO_DATE(tR.TRANDATE)
AND sO.SITE = tR.SITE
AND sO.ENTITY = tR.COMPANY
-- AND tR.TRANCLASS = 'R'

-- LEFT OUTER JOIN mart_inventory_usage AS tI
LEFT OUTER JOIN (SELECT PARTNUM, TRANDATE, SITE, COMPANY, SUM(TRANQTY) AS TRANQTY FROM mart_inventory_usage WHERE TRANCLASS = 'I' GROUP BY PARTNUM, TRANDATE, SITE, COMPANY) AS tI
ON sO.INVENTORY_ITEM_ID = tI.PARTNUM
-- AND s.DATE = TO_DATE(tI.TRANDATE)
AND date_day - 1 = TO_DATE(tI.TRANDATE)
AND sO.SITE = tI.SITE
AND sO.ENTITY = tI.COMPANY
-- AND tI.TRANCLASS = 'I'

-- LEFT OUTER JOIN mart_inventory_usage AS tA
LEFT OUTER JOIN (SELECT PARTNUM, TRANDATE, SITE, COMPANY, SUM(TRANQTY) AS TRANQTY FROM mart_inventory_usage WHERE TRANCLASS = 'A' AND TRANTYPE != 'MFG-VAR' GROUP BY PARTNUM, TRANDATE, SITE, COMPANY) AS tA
ON sO.INVENTORY_ITEM_ID = tA.PARTNUM
-- AND s.DATE = TO_DATE(tA.TRANDATE)
AND date_day - 1 = TO_DATE(tA.TRANDATE)
AND sO.SITE = tA.SITE
AND sO.ENTITY = tA.COMPANY
-- AND tA.TRANCLASS = 'A'
-- AND tA.TRANTYPE != 'MFG-VAR'

WHERE 1 = 1 
-- AND sO.INVENTORY_ITEM_ID = 'L000036-04-89-1903'
-- AND sC.INVENTORY_ITEM_ID = 'L000036-04-89-1903'
-- WHERE s.INVENTORY_ITEM_ID = '1009015-06-01-0400'
-- WHERE s.INVENTORY_ITEM_ID = '1002529-04-01-1006'
-- WHERE s.INVENTORY_ITEM_ID IN (SELECT DISTINCT PARTNUM FROM mart_inventory_usage)
ORDER BY sO.INVENTORY_ITEM_ID, sO.DATE

) AS x
GROUP BY INVENTORY_ITEM_ID, x.DATE
-- HAVING NOT (STOCK_MOVEMENT = 0 AND TRANSACTION_QUANTITY = 0)
ORDER BY INVENTORY_ITEM_ID, DATE
) AS y
GROUP BY ENTITY, SITE, INVENTORY_ITEM_ID
ORDER BY ENTITY, SITE, INVENTORY_ITEM_ID
;